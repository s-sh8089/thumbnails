services:
    front-end:
        container_name: thumbnails
        build: ./_docker/
        ports:
            - 3030:3030
        privileged: true
        volumes:
            - ./_application:/app
            - /app/node_modules
        tty: true
        stdin_open: true
        command: /bin/bash -c "yarn install && yarn dev"
